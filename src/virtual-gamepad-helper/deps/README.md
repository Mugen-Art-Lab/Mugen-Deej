# Prototype dependency directory

This directory is intentionally empty in git.

Run `tools/Prepare-HIDMaestro.ps1` to populate it locally with the pinned, hash-verified HIDMaestro SDK DLL and license. GitHub Actions does this automatically for the virtual-gamepad prototype build.
